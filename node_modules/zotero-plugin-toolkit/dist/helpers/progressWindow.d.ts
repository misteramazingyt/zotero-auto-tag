/**
 * ProgressWindow helper.
 * @example
 * Show a popup with success icon
 * ```ts
 * const tool = new ZoteroTool();
 * tool.createProgressWindow("Addon").createLine({
 *   type: "success",
 *   text: "Finish"
 *   progress: 100,
 * }).show();
 * ```
 * @example
 * Show a popup and change line content
 * ```ts
 * const compat = new ZoteroCompat();
 * const tool = new ZoteroTool();
 * const popupWin = tool.createProgressWindow("Addon").createLine({
 *   text: "Loading"
 *   progress: 50,
 * }).show(-1);
 * // Do operations
 * compat.getGlobal("setTimeout")(()=>{
 *   popupWin.changeLine({
 *     text: "Finish",
 *     progress: 100,
 *   });
 * }, 3000);
 * ```
 */
export declare class ProgressWindowHelper extends Zotero.ProgressWindow {
    private lines;
    private closeTime;
    private originalShow;
    show: typeof _popupWindowShow;
    /**
     *
     * @param header window header
     * @param options
     */
    constructor(header: string, options?: {
        window?: Window;
        closeOnClick?: boolean;
        closeTime?: number;
        closeOtherProgressWindows?: boolean;
    });
    /**
     * Create a new line
     * @param options
     */
    createLine(options: {
        type?: string;
        icon?: string;
        text?: string;
        progress?: number;
        idx?: number;
    }): this;
    /**
     * Change the line content
     * @param options
     */
    changeLine(options: {
        type?: string;
        icon?: string;
        text?: string;
        progress?: number;
        idx?: number;
    }): this;
    private showWithTimer;
    /**
     * Set custom icon uri for progress window
     * @param key
     * @param uri
     */
    static setIconURI(key: string, uri: string): void;
    private getIcon;
    private updateIcons;
}
declare function _popupWindowShow(closeTime?: number | undefined): ProgressWindowHelper;
export {};
//# sourceMappingURL=progressWindow.d.ts.map